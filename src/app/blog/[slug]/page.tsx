import { notFound } from "next/navigation"; import { Page, Section, Container } from "@/components/layout"; import { Typography, Quote, Button } from "@/components/ui"; import { posts } from "@/config/content"; import { createMetadata } from "@/seo/createMetadata";
export function generateStaticParams(){return posts.map(({slug})=>({slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const post=posts.find(x=>x.slug===slug);return post?createMetadata({title:post.title,description:post.excerpt,canonical:`/blog/${slug}`}):{};}
export default async function BlogArticlePage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const post=posts.find(x=>x.slug===slug);if(!post)notFound();return <Page><article><Section className="page-opening border-b border-stone-200"><Container><header className="editorial-intro mx-auto text-center items-center"><Typography variant="caption">{post.category}</Typography><Typography variant="h1">{post.title}</Typography><p className="text-sm text-stone-500">Arthur Pendelton · {post.date}</p></header></Container></Section><Section><Container><div className="article-copy"><Typography variant="lead">{post.lead}</Typography><h2>Durability is a design decision</h2><p>The largest cost of a system arrives after launch: in the years of reading, operating, and changing it. Novelty can be useful, but it should answer a specific constraint rather than substitute for judgment.</p><p>We favour legible structures, explicit ownership, and technologies with well-understood failure modes. This leaves teams free to focus their invention where it creates genuine advantage.</p><Quote author="Arthur Pendelton" role="Managing Partner">Boring technology can be a competitive advantage when it keeps attention on the business problem.</Quote><h2>Plan for change, not fashion</h2><p>Longevity does not mean refusing progress. It means creating seams where change is expected, measuring before replacing, and keeping the system comprehensible to the people responsible for it.</p><Button href="/blog" variant="outline">Back to insights</Button></div></Container></Section></article></Page>}
