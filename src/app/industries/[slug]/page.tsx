import { notFound } from "next/navigation"; import { Page, Section, Container } from "@/components/layout"; import { Typography, Button } from "@/components/ui"; import { industries } from "@/config/content"; import { createMetadata } from "@/seo/createMetadata";
export function generateStaticParams(){return industries.map(({slug})=>({slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const item=industries.find(x=>x.slug===slug);return item?createMetadata({title:item.title,description:item.summary,canonical:`/industries/${slug}`}):{};}
export default async function IndustryPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const item=industries.find(x=>x.slug===slug);if(!item)notFound();return <Page><Section className="page-opening bg-stone-900 text-stone-50"><Container><div className="editorial-intro"><Typography variant="caption" className="text-stone-400">Industry</Typography><Typography variant="h1" className="text-stone-50">{item.title}</Typography><Typography variant="lead" className="text-stone-300">{item.summary}</Typography></div></Container></Section><Section><Container><div className="grid gap-12 md:grid-cols-12"><div className="md:col-span-4"><Typography variant="caption">Where we focus</Typography></div><div className="md:col-span-8"><ol className="divide-y divide-stone-300 border-y border-stone-300">{item.priorities.map((x,i)=><li key={x} className="grid grid-cols-[3rem_1fr] py-7"><span className="text-sm text-stone-400">0{i+1}</span><Typography variant="h3">{x}</Typography></li>)}</ol><div className="mt-12"><Button href="/contact">Discuss your context</Button></div></div></div></Container></Section></Page>}
